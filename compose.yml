services:
  ffmpeg-tool:
    build:
      context: .
    container_name: ffmpeg-frame-extractor-api
    command: uvicorn app:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
    environment:
      PYTHONUNBUFFERED: "1"
    restart: unless-stopped
